---
import { defaultLocale, getHomeURL, type Locale } from "../../i18n/config";
import { getContent } from "../../i18n/content";

interface Props {
  locale?: Locale;
}

const { locale = defaultLocale } = Astro.props;

const t = getContent(locale);
---

<a class="logo" href={getHomeURL(locale)} aria-label={t.header.logoLabel}>
  <span class="logo-mark" aria-hidden="true">
    JM
  </span>
  <span class="logo-text" aria-hidden="true">
    Traducciones
  </span>
</a>

<style>
  .logo {
    min-height: 44px;

    display: inline-flex;
    align-items: center;

    gap: 0.65rem;

    text-decoration: none;
  }

  .logo-mark {
    width: 40px;
    height: 40px;

    display: grid;
    place-items: center;

    border-radius: var(--radius-sm);

    color: var(--color-text-inverse);

    background: var(--color-primary);

    box-shadow: inset 0 -3px 0 var(--color-accent);

    font-family: var(--font-serif);
    font-size: 1.05rem;
    font-weight: 600;

    letter-spacing: 0.02em;
  }

  .logo-text {
    color: var(--color-primary);

    font-family: var(--font-serif);
    font-size: 1.25rem;
    font-weight: 600;
  }

  @media (max-width: 360px) {
    .logo-mark {
      width: 36px;
      height: 36px;
    }

    .logo-text {
      font-size: 1.1rem;
    }
  }
</style>
